{# ============================================================
   Makro: section_heading
   ------------------------------------------------------------
   Účel:
   - Vykresluje nadpis sekce s volitelnou ikonou a poznámkou.
   - Podporuje nastavení úrovně nadpisu (h2–h6) a id atributu.
   - Používá Bootstrap utility pro zarovnání a spacing.
   Použití:
     {{ section_heading("Název sekce", icon="bi-gear", note="poznámka", level=3, id="settings") }}
   ============================================================ #}

{% macro section_heading(title, icon=None, note=None, level=2, id=None) %}
  <div class="section-heading d-flex align-items-center justify-content-between mb-3"
       {% if id %}id="{{ id }}"{% endif %}>
    
    <!-- Levá část: ikona + nadpis -->
    <div class="d-flex align-items-center gap-2">
      {% if icon %}
        <!-- Volitelná ikona (Bootstrap Icons) -->
        <i class="{{ icon }}"></i>
      {% endif %}
      <!-- Nadpis sekce, úroveň definovaná parametrem level -->
      <h{{ level }} class="mb-0">{{ title }}</h{{ level }}>
    </div>
    
    <!-- Pravá část: volitelná poznámka -->
    {% if note %}
      <span class="section-note text-muted">{{ note }}</span>
    {% endif %}
  </div>
{% endmacro %}
