{% from "components/section_heading.jinja" import section_heading %}

{# ============================================================
   Sekce: Stav aktuátorů
   ------------------------------------------------------------
   Účel:
   - Zobrazuje aktuální stav jednotlivých aktuátorů (LED, RELE, Setpoint).
   - Používá Bootstrap komponenty (badge, text-muted).
   - Hodnoty jsou aktualizovány přes JS logiku podle API.
   ============================================================ #}

<section id="actuators-status" class="mb-4" aria-labelledby="actuators-status-title">
  {{ section_heading("Stav aktuátorů", icon="bi-eye", level=5) }}
  <div class="card p-3">
    <div class="row g-3 align-items-center">

      <!-- LED status
           - Badge s id="led-status" zobrazuje aktuální stav LED.
           - Defaultní text: "Vypnuto". -->
      <div class="col-12 col-md-3">
        <label class="form-label fw-bold d-block">LED</label>
        <div id="led-status" class="badge bg-secondary">Vypnuto</div>
      </div>

      <!-- RELE status
           - Badge s id="relay-mode" zobrazuje aktuální režim relé.
           - Defaultní text: "Režim: -". -->
      <div class="col-12 col-md-5">
        <label class="form-label fw-bold d-block">RELE</label>
        <div id="relay-mode" class="badge bg-info">Režim: -</div>
      </div>

      <!-- Setpoint status
           - Hodnota nastavené teploty se zobrazuje v divu s id="setpoint-value".
           - form-text poskytuje doplňující popis. -->
      <div class="col-12 col-md-4">
        <label class="form-label fw-bold d-block">Regulovaná teplota</label>
        <div id="setpoint-value" class="fw-bold">-- °C</div>
        <div class="form-text">Aktuální nastavená teplota termostatu</div>
      </div>
    </div>
  </div>
</section>
